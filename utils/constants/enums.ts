export enum ESection {
  SUMMARY = 0,
  UPCOMING = 1,
  HISTORIC = 2,
}

export enum EMatchType {
  HISTORIC = "historic",
  UPCOMING = "upcoming",
}

export enum ETeamComponentMode {
  ROW = "row",
  COLUMN = "column",
}
